#!/bin/sh
# Laughably simple configure script

if uname | grep -i linux &>/dev/null; then
    ln -s ./Makefile.linux hunt/Makefile
    ln -s ./Makefile.linux huntd/Makefile
elif uname | grep -i bsd &>/dev/null; then
    ln -s ./Makefile.bsd hunt/Makefile
    ln -s ./Makefile.bsd huntd/Makefile
fi
